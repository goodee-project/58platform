<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="goodee.gdj58.platform.mapper.ReportMapper">
	<select id="selectReport"
	parameterType="String" 
	resultType="goodee.gdj58.platform.vo.Report">
	 SELECT
	 	report_no reportNo
	 	, report_category reportCategory
	 	, report_content reportContent
	 	, report_memo reportMemo
	 	, createdate
	 FROM 
	 	report
	 	<if test="#{reportCategory == '쇼핑'}">
	 		INNER JOIN shopping_goods_order
	 		ON re
	 	</if>
	 WHERE
	 	report_category = #{reportCategory}
	</select>

</mapper>